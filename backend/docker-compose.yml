version: '3'
services:
  webdav:
    image: bytemark/webdav
    restart: always
    ports:
      - "80:80"
    environment:
      AUTH_TYPE: Basic
      USERNAME: mclouduser
      PASSWORD: mcloudpass123
    volumes:
      - ./files:/var/lib/dav
